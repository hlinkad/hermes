"""Tool and adapter registry extension point.

Future registry work discovers `ToolManifest` and `ProviderSpec` declarations
without embedding concrete tool behavior into `brain_lab_core`.
"""
from __future__ import annotations

__all__: list[str] = []
